extends layout.pug 

block title 	
	<title>#{title}</title>

block content 	
	h2 #{title}
	if emailsVisible 
		h3 Электронные адреса
		ul
			each email in emails 
				li #{email}
	else 
		h3 Электронные адреса отсуцтвуют
	h3 Телефонный номер
	#{phone}